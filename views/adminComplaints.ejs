<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Complaints - <%= hostel %></title>
    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>
    <div class="container mt-5">
        <h1 class="mb-4 text-center">Complaints for Hostel: <%= hostel %></h1>

        <table class="table table-striped table-bordered">
            <thead class="table-dark text-center">
                <tr>
                    <th>Student Name</th>
                    
                    <th>Description</th>
                    <th>Action</th>
                </tr>
            </thead>
            <tbody>
                <% if (complaints.length > 0) { %>
                    <% complaints.forEach(complaint => { %>
                        <tr>
                            <td><%= complaint.author.first_name %> <%= complaint.author.last_name %></td>
                           
                            <td><%= complaint.description %></td>
                            <td class="text-center">
                                <% if (complaint.status === "pending") { %>
                                    <form action="/admin/complaints/<%= complaint._id %>/resolved" method="POST" class="d-inline">
                                        <button type="submit" class="btn btn-sm btn-success">Mark as Complete</button>
                                    </form>
                                <% } else { %>
                                    <span class="badge bg-success">Completed</span>
                                <% } %>
                            </td>
                        </tr>
                    <% }) %>
                <% } else { %>
                    <tr>
                        <td colspan="4" class="text-center">No complaints available</td>
                    </tr>
                <% } %>
            </tbody>
        </table>
    </div>

    <!-- Bootstrap JS -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
